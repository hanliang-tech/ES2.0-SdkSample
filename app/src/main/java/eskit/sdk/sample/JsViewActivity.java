package eskit.sdk.sample;import android.os.Bundle;import android.view.KeyEvent;import android.widget.FrameLayout;import androidx.annotation.Nullable;import androidx.appcompat.app.AppCompatActivity;import com.extscreen.runtime.api.ability.slotview.AttachStateCallback;import com.sunrain.toolkit.utils.ToastUtils;import eskit.sdk.core.EsData;import eskit.sdk.core.jsview.JsSlotView;import eskit.sdk.core.jsview.JsSlotViewManager;/** * <br> * * <br> */public class JsViewActivity extends AppCompatActivity {    private FrameLayout mContainer;    private boolean mLoadSuccess;    @Override    protected void onCreate(@Nullable Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        super.setContentView((mContainer = new FrameLayout(this)));        loadJsViewApp();    }    private void loadJsViewApp() {        EsData data = new EsData();        data.setAppPackage("tv.huan.slot.demo");//        data.setAppPackage("debug");//        data.setAppDownloadUrl("http://192.168.7.175:38989");        JsSlotViewManager.get().attachToActivity(this, data, (stateCode, errorMessage) -> {            mLoadSuccess = stateCode == AttachStateCallback.STATE_SUCCESS;            ToastUtils.showShort(mLoadSuccess ? "加载成功" : "加载失败 " + errorMessage);        });    }    @Override    public boolean onKeyDown(int keyCode, KeyEvent event) {        if (mLoadSuccess && keyCode == KeyEvent.KEYCODE_MENU) {            JsSlotView view = JsSlotViewManager.get().createJSView(this, "clock");            // 如果卡片本身内部有焦点操作            view.setNeedFocus(true);            mContainer.addView(view);            return true;        }        return super.onKeyDown(keyCode, event);    }}